fields:
    name:
        label: 'mavitm.content::lang.content.categories.name'
        span: left
        required: 1
        type: text
    slug:
        label: 'mavitm.content::lang.content.categories.slug'
        span: right
        preset:
            field: name
            type: slug
        type: text
    description:
        label: 'mavitm.content::lang.content.categories.description'
        size: large
        span: full
        type: richeditor
    category_type:
        label: 'mavitm.content::lang.post.content_type'
        type: dropdown
    image:
        label: 'mavitm.content::lang.post.list_image'
        mode: image
        useCaption: true
        thumbOptions:
            mode: crop
            extension: auto
        span: full
        type: fileupload
        tab: 'mavitm.content::lang.post.pictures'